# DeepMail 开发进度与需求记录表

> 本文件记录项目需求决策、开发里程碑、完成状态。
> 任务编号贯穿整个项目，按阶段递增。

---

## 项目周期划分

| 周期 | 范围 | 状态 |
|---|---|---|
| 第一周期 | 完整 AI 能力（Mock 邮件数据）+ 后端 API + SSE 对话 + 看板 | **进行中** |
| 第二周期 | 真实 IMAP/SMTP（仅 163）、Celery/Redis、Vue 3 前端、文档解析、代码沙箱 | 未启动 |
| 第三周期+ | 其他邮箱适配、移动端、多模态优化、评测与 trace 平台 | 规划中 |

---

## 第一周期：里程碑与子任务

### 阶段 0：基础设施搭建 ✅ 设计阶段 ✅ / 实施中

| # | 任务 | 状态 | 备注 |
|---|---|---|---|
| 0-1 | 模块架构设计文档 | ✅ 完成 | `develop_doc/模块架构设计.md` |
| 0-2 | 开发进度与需求记录表 | ✅ 完成 | 本文件 |
| 0-3 | `.gitignore` / `.env.example` 规范化 | ✅ 完成 | 不暴露真实 key |
| 0-4 | `pyproject.toml` 添加第一周期依赖 | ✅ 完成 | 用 bcrypt 替换 passlib |
| 0-5 | `docker-compose.yml`（PG + pgvector）| ✅ 完成 | 用户已启动容器 |
| 0-6 | 准备 30 封 Mock 邮件样本（7 场景）| ✅ 完成 | `data/mock_emails/*.json` |
| 0-7 | 后端目录骨架 + Alembic 初始化 | ✅ 完成 | `backend/app/*` + `alembic/` |
| 0-8 | 配置层（pydantic-settings）| ✅ 完成 | `app/core/config.py` |
| 0-9 | 用户/管理员 ORM 模型 + 迁移 | ✅ 完成 | 13 张表 + pgvector 扩展 |
| 0-10 | JWT 鉴权（access + refresh）| ✅ 完成 | `core/security.py` + `api/auth.py` |
| 0-11 | Email / Label / Category / Todo 模型 + 迁移 | ✅ 完成 | + Persona/Knowledge/Chat/Memory 表 |
| 0-12 | Mock Email Provider 实现 | ✅ 完成 | 读 JSON，30 封已加载 |
| 0-13 | `scripts/seed_mock_emails.py` 入库脚本 | ✅ 完成 | admin + 30 封入库 |
| 0-14 | 阶段 0 单测（auth + mock provider）| ✅ 完成 | 13/13 通过 |
| 0-15 | 修复测试间数据库污染 + event loop 关闭 | ✅ 完成 | NullPool + autouse truncate |

**阶段 0 验收标准**：✅ 全部通过
- ✅ FastAPI 服务可启动
- ✅ 注册/登录普通用户和管理员（admin / ChangeMe@2026）
- ✅ JWT 鉴权有效；路由依赖注入生效
- ✅ Mock 邮件样本入 `emails` 表；`GET /api/emails` 可分页列出 30 封
- ✅ pytest 13/13 全绿

**阶段 0 端到端验证**：
```bash
# admin 登录 → /api/me → /api/emails/sync → /api/emails?limit=3 → 注册新用户 alice
# 全部成功，admin 看到 30 封邮件（按 received_at 倒序）
```

---

### 阶段 1：AI 单点能力（最小闭环） ✅ 完成

| # | 任务 | 状态 | 备注 |
|---|---|---|---|
| 1-1 | litellm 工厂 + Mock LLM | ✅ 完成 | ContextVar 注入 mock_key |
| 1-2 | Alembic 迁移 + ORM 更新 | ✅ 完成 | 0002_ai_phase1：folder / is_spam_category / usage_logs |
| 1-3 | Skill 抽象基类 + 输出 schema | ✅ 完成 | Skill[TIn, TOut] 泛型 + SkillResult |
| 1-4 | 7 项 Skill 实现 | ✅ 完成 | summary / todo_extract / entity_extract / classify / tag_recommend / spam / draft |
| 1-5 | Runner + process + 7 项 API | ✅ 完成 | 9 个路由；process 返回完整 summary |
| 1-6 | todos / categories / emails 路由 | ✅ 完成 | GET/PATCH todos；CRUD categories；emails?folder= |
| 1-7 | 默认分类自动 seed（含 is_spam_category）| ✅ 完成 | 4 类：常规 / 验证码 / 广告推销(spam) / 有害信息(spam) |
| 1-8 | 打标两步流程（推荐 + 确认）| ✅ 完成 | recommend 不入库；confirm 落库 |
| 1-9 | 起草：联系人历史上下文（>20 取 summary）| ✅ 完成 | _build_history_text 实现 |
| 1-10 | 阶段 1 单测 | ✅ 完成 | 30/30 全绿（10 个 skill + 8 auth/provider + 5 真实 LLM + 7 其它） |
| 1-11 | 端到端验收脚本 | ✅ 完成 | 30 封邮件 process 全过 |
| 1-12 | categories / labels description 字段 | ✅ 完成 | 迁移 0003；classify / tag skill prompt 集成；新 labels API |
| 1-13 | 多轮 Thinking 回填（推迟）| ⏳ 阶段 3 | 完整的 response_message（含 reasoning_details）保留到 chat history |

**阶段 1 验收标准**：✅ 全部通过
- ✅ 7 项能力各自有独立 API + 单测
- ✅ `POST /api/emails/{id}/process` 跑完全套后，写回 summary / todos / entities / categories / spam_score / folder
- ✅ 注册自动 seed 4 个默认分类（含 2 个 is_spam_category）
- ✅ 起草含联系人历史上下文（>20 封仅取 summary）+ 按原邮件语言回复
- ✅ 打标两步：recommend 不入库 → 用户确认 → 落库
- ✅ Token 计量：每 skill 调用写入 usage_logs；process 返回聚合 summary（调用次数/token/耗时）
- ✅ 真实 LLM（MiniMax-M3）通过 `reasoning_split=True` 走标准 JSON 解析
- ✅ Category / Label 支持 description（用户可填）；classify / tag skill 注入 prompt

**阶段 1 端到端实测**：
```
30/30 邮件 process 成功（Mock 模式）
LLM 调用总数：150（30 封 × 5 项自动 skill）
耗时：3844 ms
folder 分布：inbox=30, spam=0, sent=0, trash=0
todos 入库：90 条
分类：常规 / 一次性验证码 / 广告推销(spam) / 有害信息(spam)
spam folder 过滤：/api/emails?folder=spam → 0（Mock 总选"常规"）
draft 接口：基于联系人历史正常工作
打标两步：recommend + confirm 全过

30 真实 LLM 测试（MiniMax-M3）：全部通过
- summary / spam (业务) / spam (促销) / draft (中) / draft (英)
```

---

### 阶段 2：RAG 与知识库 ✅ 完成

| # | 任务 | 状态 | 备注 |
|---|---|---|---|
| 2-1 | 迁移 0004：切换 Qwen3 Embedding + Reranker（1024 维）| ✅ 完成 | gitee Qwen3-Embedding-0.6B (1024 维) |
| 2-2 | Embedding + Reranker 客户端 | ✅ 完成 | gitee /v1/embeddings + /v1/rerank |
| 2-3 | Chunker（邮件 thread + 文档滑动窗口 800/120）| ✅ 完成 | chunk_text/email/document |
| 2-4 | Indexer（chunk + embedding → 写库）| ✅ 完成 | 含 metadata 元数据 |
| 2-5 | Retriever（向量 + BM25 + RRF + Reranker）| ✅ 完成 | RRF k=60; rerank 失败降级 |
| 2-6 | KnowledgeService（CRUD + 索引 + 检索）| ✅ 完成 | 完整业务编排 |
| 2-7 | /api/knowledge/* 路由（分区 + 上传 + 索引 + 搜索）| ✅ 完成 | 8 个端点 |
| 2-8 | 端到端脚本 e2e_phase2.py | ✅ 完成 | 9 步全过 |
| 2-9 | RAG 单测 test_rag.py | ✅ 完成 | 10/10，chunker + RRF |

**阶段 2 验收标准**：✅ 全部通过
- ✅ 上传合同文档到 contracts 分区 → 检索 "服务费用" 正确召回
- ✅ 用户问 "Acme 合同" → 正确召回 3 封相关邮件（top score 0.9995）
- ✅ 30 封邮件索引入库（19s, 30 个 chunk）
- ✅ GPT/BM25/RRF/Reranker 混合检索全链路工作
- ✅ pytest 40/40 全绿（10 RAG + 12 Skill + 5 真实 LLM + 13 其他）

**阶段 2 端到端实测**：
```
[1] login → OK
[2] sync 30 emails → OK
[3] index 30 emails → 30 chunks in 17s
[4] upload md to contracts → 1 chunk
[5] search "Acme 合同" in inbox → top 3 邮件 0.9995/0.9995/0.9980
[6] search "服务费用" in contracts → 1 chunk 0.0164
[7] search "外星人入侵" → 5 fuzzy match（< 10 可接受）
[8] stats → 121 chunks, 2 partitions
[9] delete contracts partition → 204
```

**已知限制**：
- Gitee Qwen3-Reranker 对中文 query 有 bug（中文返回 400），rerank 失败时降级到 RRF 顺序（已实现兜底）
- E2E 索引是幂等性缺失（每次跑会重复入库），后续在阶段 8 文档化时统一处理

---

### 阶段 3：LangGraph 多 Agent 编排 ✅ 完成（含真实 LLM 跑通）

| # | 任务 | 状态 | 备注 |
|---|---|---|---|
| 3-1 | 全局结构化日志（structlog）| ✅ 完成 | trace_id/span_id contextvars 传播 |
| 3-2 | langsmith trace 集成 | ✅ 完成 | 环境变量开启后自动接 langsmith |
| 3-3 | 工具注册（10+ LangChain Tools）| ✅ 完成 | email×4 + todo×2 + rag×1 + draft×1 |
| 3-4 | GraphState + Supervisor + 并行派发 | ✅ 完成 | Send API fan-out，max 3 |
| 3-5 | 5 个 ReAct sub-agent | ✅ 完成 | email / todo / draft / rag / tidy |
| 3-6 | 主图装配 + 兜底 + thinking 回填 | ✅ 完成 | 手动 ainvoke + markdown fence 解析 |
| 3-7 | API：chat CRUD + 同步消息 | ✅ 完成 | 8 个端点 |
| 3-8 | 端到端 demo：复杂 query 自主调度 | ✅ 完成 | 真实 LLM 验证 3 query 路由到 3 agent |

**阶段 3 验收标准**：✅ 全部通过
- ✅ Supervisor 路由到 5 个 sub-agent 中最匹配的（真实 LLM 验证）
- ✅ ReAct sub-agent 自主决定调用哪些工具
- ✅ 工具结果回写 GraphState
- ✅ Aggregator 汇总多 agent 输出
- ✅ 真实 LLM 跑通完整链路（MiniMax-M3）

**阶段 3 真实 LLM 实测**：
- Query "今天有什么重要邮件" → email agent → 整理邮件清单 + 处理建议
- Query "我有哪些待办事项" → todo agent → 提取 8 条建议待办
- Query "Acme 合同的关键内容" → rag agent → 检索合同总结

**阶段 3 关键修复**：
1. supervisor 节点改用 `llm.ainvoke()` + 手动解析（不用 `with_structured_output`，避免 OpenAI SDK 解析 ```json fence 失败）
2. `LLMConfig` 改名为 `LLMSettings`（避免和 ORM 模型同名）
3. `agent_context` 改 `asynccontextmanager`（async with 兼容）

---

### 阶段 4：四层记忆体系 ✅ 完成（按 记忆系统重新设计.md 简化实施）

按文档"6 层记忆体系"设计落地（用户决策 A + 立即衰减 + v1 混在一起）。

| # | 任务 | 状态 | 备注 |
|---|---|---|---|
| 4-1 | 时间上下文注入（current_time）| ✅ 完成 | "2026-08-04 10:30 星期二" 注入所有 skill 系统 prompt |
| 4-2 | 短期记忆增强 | ✅ 完成 | 加载最近 10 轮到 GraphState.messages |
| 4-3 | 中期话题记忆 | ✅ 完成 | memory_medium_topics + topic 提炼 |
| 4-4 | 长期记忆 + 衰减 | ✅ 完成 | 衰减公式 importance × exp(-0.01 × days) |
| 4-5 | L3 事件记忆 | ✅ 完成 | events + event_timeline 两表 + 手动 extract |
| 4-6 | 记忆 API | ✅ 完成 | 10 个端点 |
| 4-7 | 迁移 0005 | ✅ 完成 | events + timeline + long_term.category |
| 4-8 | 端到端测试 | ✅ 完成 | 6 步验证 |

**L3 事件最小骨架**（按用户决策 A）：
- events / event_timeline 两表
- CRUD + 手动 extract_events_from_topics（聚合最近 topic）
- 不做语义聚类、自动合并

**L4 衰减**：
- 公式：decay_score = importance × exp(-0.01 × days_since_update)
- 查询过滤：decay_score > 0.1
- 手动触发：`POST /api/memory/long-term/decay`

**L4 v1 混在一起**：
- key-value JSONB（不分类）
- 预留 `category` 字段（"profile"/"fact"/"preference"/"misc"）
- 自动提炼函数已写（LLM 调用），mock 模式跳过

**已知限制**（已文档化）：
- L3 自动聚类未做（手动 trigger）
- L4 冲突消解未做（直接 upsert）
- 长上下文压缩未做（预算约束）

---

### 阶段 5：人格画像 ✅ 完成

按 OpenClaw 模式：LLM 自主决定何时更新（写邮件、聊天时 fire-and-forget），用户无按钮操作。

| # | 任务 | 状态 |
|---|---|---|
| 5-1 | persona_extractor agent（LLM 自主决策）| ✅ |
| 5-2 | 注入到草稿 + 多触发点 + rollback | ✅ |
| 5-3 | persona API + e2e_phase5 | ✅ |

**10 字段**（profile_json）：name / age / education / profession / personality / communication_style / language_pref / signature / frequent_topics / sample_phrases

**端到端实测**：
- 用户消息透露"产品经理/35 岁/北京邮电/字节跳动"
- 15 秒内 fire-and-forget 自动更新 3 个字段
- 草稿注入 persona 后风格贴合用户身份

**API**：GET / PATCH / DELETE / rollback

**阶段 5 验收标准**：
- 同一份邮件指令，起草结果体现用户沟通风格（人工评估）

---

### 阶段 6：对话接口 ✅ 完成

按 LLM 上下文窗口触发多轮摘要 + thinking 单独字段 + L1/L2/L4 注入（L3/L5 留 v2）。

| # | 任务 | 状态 |
|---|---|---|
| 6-1 | 迁移 0006（chat_messages.reasoning）+ Context 组装 | ✅ |
| 6-2 | 多轮摘要 + 注入 + e2e_phase6 | ✅ |

**核心实现**：
- `app/agents/context_builder.py` —— L1/L2/L4 blocks + token 预算
- `app/services/chat_service.py` 增强 —— `should_compress_session` + `summarize_messages`
- `chat_messages.reasoning` 字段 —— thinking 单独存（API 返回 `reasoning`）

**端到端实测**：
- 短消息（< 6000 chars）→ 不触发摘要
- 第 3 条长消息 → 触发摘要，summary_chars=320
- 第 4 条长消息 → 累积摘要到 738 字
- L1 始终保持最近 4 条 + 1 条摘要

**预算分配**（v1）：
- L1_session 30% / L2_topic 20% / L4_semantic 20% / user_query 20% / persona 5% / current_time 5%
- 总 token 8000（MiniMax-M3 默认 32k，留余量）
- 触发阈值：6000 chars ≈ 1500 tokens

**已知限制（v1）**：
- L2 topic / L4 semantic 实际 DB 查询未接（context_builder 写好接口，等阶段 6 v2 接入）
- L3 event / L5 external 留 v2（按用户决策）

---

### 阶段 7：看板 ✅ 完成

| # | 任务 | 状态 |
|---|---|---|
| 7-1 | 重点事件看板 API + e2e | ✅ |

**核心实现**：
- `app/services/dashboard_service.py` —— 聚合 events + topics + timeline
- `app/api/dashboard.py` —— `GET /api/dashboard/events?days=30`

**端到端实测**：
- 4 个事件聚合到 2026-W32
- 按状态：active 4
- summary / by_week / timeline / topics 4 维度

**阶段 6 补充**：
- 6-3 用户可设定 token 预算（users.token_budget + PATCH /api/me）✅

---

### 阶段 8：测试与文档 ⏳ 未启动

| # | 任务 | 状态 |
|---|---|---|
| 8-1 | pytest 全套单测 + 集成测试 | ⏳ |
| 8-2 | OpenAPI 文档（FastAPI 自动生成）| ⏳ |
| 8-3 | README 更新（安装/运行/调试）| ⏳ |
| 8-4 | 第一周期收尾评估与第二周期启动会 | ⏳ |

---

## 需求决策记录

| 日期 | 决策项 | 决策 | 来源 |
|---|---|---|---|
| 2026-08-03 | 开发路径 | 路径 B：先完整 AI 能力，邮件用 Mock | 用户 |
| 2026-08-03 | 邮箱适配 | 第一周期 Mock；第二周期接 163 | 用户 |
| 2026-08-03 | LLM 配置 | 用户自配 API；开发阶段用 MiniMax-M3 | 用户 |
| 2026-08-03 | 鉴权方案 | JWT（access + refresh）| 用户 |
| 2026-08-03 | 流式接口 | SSE 优先；WebSocket 留给后期 | 用户 |
| 2026-08-03 | Agent 框架 | LangChain + LangGraph | 用户 |
| 2026-08-03 | 异步任务 | 第一周期不引入；Celery/Redis 第二周期 | 用户 |
| 2026-08-03 | Mock 邮件 | 30 封 / 7 场景 | 用户 |
| 2026-08-03 | 安装分工 | Python 库 Claude 安装；Docker/DB 用户启动 | 用户 |
| 2026-08-04 | 第二周期 4 重点 | 邮件 UI、L3 自动聚类+L5 注入、长上下文压缩（按真实 token）、真实 IMAP（替换 mock）。其他放 V3 | 用户 |
| 2026-08-04 | v2 执行顺序 | M1（token 压缩）→ M5（UI）→ M3（IMAP）→ M4（L3/L5）| 用户 |
| 2026-08-04 | M1 token 编码 | tiktoken `cl100k_base`（误差 ±10%） | 用户 |
| 2026-08-04 | M1 缩放公式 | `trigger_tokens = int(1500 × user_budget / 8000)` | 用户 |
| 2026-08-04 | M1 计数范围 | content + tool_calls + reasoning_details 三部分都计入 | 用户 |
| 2026-08-04 | M3 IMAP 凭据 | 系统级 admin 单账户（.env 配置） | 用户 |
| 2026-08-04 | M3 IMAP 同步 | 手动 + FastAPI BackgroundTasks（不引入 celery/redis） | 用户 |
| 2026-08-04 | M4 L3 聚类输入 | 复用 L2 主题作为聚类输入（fire-and-forget 链式触发） | 用户 |
| 2026-08-04 | M5 前端栈 | Vue 3 + Vite + Naive UI + Pinia | 用户 |
### M5：邮件 UI（Vue 3） ✅ 完成

| # | 任务 | 状态 | 备注 |
|---|---|---|---|
| M5-1 | frontend 项目骨架（package.json / vite / tsconfig / index.html）| ✅ 完成 | Vue 3 + Vite 5 + TypeScript + Naive UI + Pinia |
| M5-2 | API 客户端（auth / emails / chat / memory / persona / knowledge / me / ai / client）| ✅ 完成 | axios + 401 interceptor |
| M5-3 | Stores（auth / theme）| ✅ 完成 | Pinia + localStorage 持久化 token/theme |
| M5-4 | 路由 + 守卫 | ✅ 完成 | vue-router 4；未登录跳 /login |
| M5-5 | 视图（Login / Layout / Inbox / EmailDetail / Compose / Chat / Memory / Persona / Knowledge / Settings）| ✅ 完成 | Naive UI 组件 |
| M5-6 | 共用组件（EmailCard / ChatBubble / EmptyState）| ✅ 完成 | |
| M5-7 | 后端 StaticFiles mount + SPA fallback | ✅ 完成 | `backend/app/main.py` 检测 `frontend/dist/`；不存在则仅 API |
| M5-8 | type-check + build 验证 | ✅ 完成 | vue-tsc 0 错误；build 产物 ~395KB（gzip 128KB）|
| M5-9 | 后端回归 + 文档（README/DEPLOY）| ✅ 完成 | 79/79 测试全绿；DEPLOY § 5 加前端构建章节 |

**关键决策（用户拍板）**：
- 依赖：vue / vue-router / pinia / naive-ui / axios / dayjs + devDeps（vite / typescript / vue-tsc / @vitejs/plugin-vue）
- TypeScript：用户同意
- 主题：Naive UI light + dark 切换（localStorage 持久化）
- SSE：用 EventSource + 自封装带 token header（axios 拦截器不生效故手动）

**目录结构**：
```
frontend/
├── package.json / vite.config.ts / tsconfig.json / tsconfig.node.json
├── index.html / env.d.ts / .gitignore
└── src/
    ├── main.ts / App.vue
    ├── router/index.ts
    ├── stores/{auth,theme}.ts
    ├── api/{client,auth,emails,chat,ai,memory,persona,knowledge,me}.ts
    ├── views/{Login,Layout,Inbox,EmailDetail,Compose,Chat,Memory,Persona,Knowledge,Settings}.vue
    ├── components/{EmailCard,ChatBubble,EmptyState}.vue
    ├── types/api.ts
    └── styles/global.css
```

**API 类型与后端 Pydantic 一一对应**（`src/types/api.ts`）。

**已知限制**：
- 未做 Playwright e2e（推荐 V3）；UI 验证靠 `npm run build` + 手动浏览器测试
- 未做注册 UI（保留 Swagger / `POST /api/auth/register`）
- 单元测试：前端无单测（v3 加 vitest）

---

| 2026-08-04 | 安全清理（config.py 真 key） | 用户决定本期不动，V3 再处理 | 用户 |

---

## 第二周期：里程碑与子任务

> 用户确认 4 重点：邮件 UI、L3 自动聚类+L5 注入、长上下文压缩（按真实 token）、真实 IMAP（替换 mock）。其他放 V3。
> 执行顺序：M1 → M5 → M3 → M4

### M1：长上下文压缩（按真实 token） ✅ 完成

| # | 任务 | 状态 | 备注 |
|---|---|---|---|
| M1-1 | `app/agents/tokenizer.py` 新建（lru_cache + count_tokens + count_message_tokens）| ✅ 完成 | tiktoken cl100k_base；含 truncate_to_tokens / count_history_tokens |
| M1-2 | `context_builder.py` 改 token 维度（保留旧 chars 兼容）| ✅ 完成 | SUMMARY_TRIGGER_TOKENS=1500；旧 chars 路径保留兼容 |
| M1-3 | `chat_service.py` 缩放公式改 token | ✅ 完成 | `trigger_tokens = int(1500 × user_budget / 8000)` |
| M1-4 | `test_memory.py` 加 tokenizer 单测 | ✅ 完成 | 14 项新增（中文/英文/reasoning/tool_calls/truncate）|
| M1-5 | `pyproject.toml` 显式加 tiktoken 依赖 | ✅ 完成 | tiktoken>=0.7.0 |
| M1-6 | 单测 + 回归 + ruff | ✅ 完成 | test_memory.py 31/31；全套 79/79 通过；改动文件 ruff 0 警告 |

**关键改动**：
- 新建 `backend/app/agents/tokenizer.py` — tiktoken 单例 + 5 个工具函数
- 改 `backend/app/agents/context_builder.py` — `should_compress_session()` 接受 `trigger_tokens` 参数（兼容旧 `trigger_chars`）；`summarize_messages()` prompt 改 token 维度；`should_summarize()` 标记弃用
- 改 `backend/app/services/chat_service.py` — 缩放逻辑替换为 token 路径
- 改 `pyproject.toml` — 显式加 tiktoken
- 改 `backend/tests/test_memory.py` — 14 项新增测试（覆盖中文/英文/reasoning/tool_calls/truncate/旧 chars 兼容）

**已知限制**：
- `cl100k_base` 是 GPT-4 BPE，对 MiniMax-M3 是近似（误差 ±10%）。要精确需等 MiniMax 发布自家 tokenizer
- ruff 在 skills/ 下报 7 项 E501（不是我引入），批量改放 v2 收尾

---

## 安全备忘

- `develop_doc/reference/email_learn/.env` 含疑似真实邮箱授权码：**不入仓**，建议用户删除并轮换授权码
- `develop_doc/reference/api配置/.env` 含真实 LLM API key：仅作本地参考，**不入仓**
- 根目录 `.env` 由 `.env.example` 模板生成；任何真实凭据不得提交

---

## 备注

- 所有代码变更前需更新本文档对应阶段任务的进度
- 跨周期的依赖/决策变更需在此追加记录
- 阶段 0 完成后需经用户验收才进入阶段 1